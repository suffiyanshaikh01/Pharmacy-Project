eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 9090
spring:
  application:
    name: WEBPORTAL-SERVICE
  mvc:
    view:
      prefix: /WEB-INF/JSP/
      suffix: .jsp
